import { Injectable } from '@angular/core';

/**
 * This service class gets the data from an FMS end point and provides functions for that data
 */
@Injectable({
  providedIn: 'root'
})
export class FmsDataService {

  constructor() { }

  // TODO: Create a method, which retrieves a FMS JSON from the server

  // TODO: Create a method, which converts the JSON into an FMSData object
}
