{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://spaceteam.at/schemas/pair-array.schema.json",
  "title": "pairArray",
  "description": "An array of pairs",
  "type": "array",
  "minItems": 1,
  "items": {
    "$ref": "#/definitions/pair"
  },
  "definitions": {
    "pair": {
      "title": "pair",
      "description": "A key-value pair containing a key as description and its value",
      "type": "object",
      "properties": {
        "key": {
          "description": "The unique key",
          "type": "string"
        },
        "value": {
          "description": "The key's value",
          "type": "string"
        },
        "valueType": {
          "description": "An enum which contains supported data types",
          "type": "string",
          "enum": ["number", "boolean", "string", "date", "pairArray"],
          "minLength": 1
        }
      },
      "required": ["key", "value", "valueType"]
    },
    "pairArray": {
      "title": "pairArray",
      "description": "An array of pairs",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/pair"
      }
    }
  }
}
